const initialFeedsState = {
  isLoading: false,
  isLoaded: false,
  eventFeeds: []
}

function eventFeeds(state = initialFeedsState, action) {
  switch (action.type) {
  default:
    return state
  }
}

export default eventFeeds
